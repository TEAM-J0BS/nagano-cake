<div class="genresearch">
  <table class = "table table-bordered"> 
    <tr><th>ジャンル検索</th></tr>
    <% @genres.each do |genre| %>
      <tr><td><%= link_to genre.name, genre_path(genre), class: "link" %></td></tr>
      <% end %>
  </table>
</div>
